azure-ai-inference==1.0.0b5  # Replace with the correct version if different
azure-core==1.30.0         # Update if needed to match azure-ai-inference
python-dotenv==1.0.0       # Optional, if using .env files
